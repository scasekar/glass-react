---
phase: 17-react-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/glass-renderer.spec.ts
autonomous: true
requirements:
  - REACT-02
  - GLASS-05

must_haves:
  truths:
    - "Playwright test suite includes a test that loads the full demo app (not harness) and verifies GlassPanel renders non-black pixels over the live C++ background"
    - "Playwright test suite includes a resize test that verifies non-black output after resize (GLASS-05 smoke)"
    - "All Playwright tests pass with npm run test:e2e after Plan 03 completes (tests are written now, will be run as phase gate)"
    - "Screenshot saved to tests/screenshots/ for manual review"
  artifacts:
    - path: "tests/glass-renderer.spec.ts"
      provides: "E2E screenshot verification for live integration"
      contains: "glass-integration"
  key_links:
    - from: "tests/glass-renderer.spec.ts"
      to: "http://localhost:5174"
      via: "Playwright page.goto('/')"
      pattern: "goto.*/"
---

<objective>
Extend the existing Playwright spec to add integration tests that verify `GlassPanel` renders visibly over the live C++ noise/image background after Phase 17 wiring. These tests are written now (Wave 1) so they are ready to run as the phase gate after Plan 03 completes.

Purpose: The phase feedback requirement mandates automated screenshot verification — not just human visual review. The existing harness tests verify `GlassRenderer` in isolation; this plan adds tests that verify the full React integration path.
Output: Extended `tests/glass-renderer.spec.ts` with `glass-integration` describe block. Tests will fail until Plan 03 wires `GlassProvider` (expected — they serve as the automated phase gate).
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-react-integration/17-RESEARCH.md
@tests/glass-renderer.spec.ts
@playwright.config.ts

<interfaces>
<!-- Playwright config: testDir: './tests', baseURL: 'http://localhost:5174' -->
<!-- webServer: 'npm run dev:demo', port: 5174 -->
<!-- launchOptions: ['--enable-unsafe-webgpu', '--use-angle=default'] -->

<!-- Existing spec pattern (from tests/glass-renderer.spec.ts): -->
<!-- Uses HARNESS_URL = '/?harness' for harness tests -->
<!-- Waits for '#glass-renderer-canvas' via waitForSelector -->
<!-- Counts non-black pixels by sampling screenshot PNG buffer (every 40th byte = every 10th RGBA pixel) -->
<!-- Non-black threshold: pixel channel > 20 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glass-integration describe block to Playwright spec</name>
  <files>tests/glass-renderer.spec.ts</files>
  <action>
    Append a new `test.describe('GlassProvider integration', ...)` block to the END of `tests/glass-renderer.spec.ts`. Do NOT modify the existing `GlassRenderer visual harness` block.

    The new block must contain these 4 tests:

    **Test 1: GlassPanel renders non-black pixels over live background**
    - URL: `'/'` (root demo app, NOT `/?harness`)
    - Wait for `#gpu-canvas` (the GlassProvider canvas, not `#glass-renderer-canvas`)
    - Also wait for `[data-testid="glass-panel"]` to be visible (or attached — a GlassPanel in the demo)
    - `await page.waitForTimeout(2000)` to allow WASM init + GlassRenderer pipeline compilation + rAF frames
    - Screenshot the full page: `await page.screenshot({ path: join(screenshotDir, 'glass-integration.png') })`
    - Sample the `#gpu-canvas` element screenshot for non-black pixels (same sampling pattern as existing tests: every 40th byte, threshold > 20)
    - Assert: non-black ratio > 0.1 (10%)
    - Log: `Non-black ratio (integration): ${(ratio * 100).toFixed(1)}%`

    **Test 2: GlassPanel element is visible in DOM**
    - URL: `'/'`
    - Wait for `#gpu-canvas` visible
    - Assert: `page.locator('[data-testid="glass-panel"]')` is visible (or attached if not visible due to z-index)
    - This covers REACT-02 (public API: GlassPanel still present)

    **Test 3: Resize does not produce black canvas (GLASS-05 smoke)**
    - URL: `'/'`
    - Wait 2000ms for full init
    - `await page.setViewportSize({ width: 1200, height: 800 })`
    - `await page.waitForTimeout(500)` for ResizeObserver + bind group refresh
    - Screenshot `#gpu-canvas` and assert non-black ratio > 0.1
    - Log: `Post-resize non-black ratio (integration): ...`

    **Test 4: Save integration screenshot for manual review**
    - URL: `'/'`
    - Wait 2000ms
    - Save full-page screenshot to `tests/screenshots/glass-integration-full.png`
    - Assert file exists

    **beforeEach setup for this describe block:**
    ```typescript
    test.beforeEach(async ({ page }) => {
      await page.goto('/');
      await page.waitForSelector('#gpu-canvas', { state: 'visible', timeout: 15_000 });
      await page.waitForTimeout(2000);
    });
    ```

    Use `join(process.cwd(), 'tests', 'screenshots')` for screenshot directory (matches existing spec pattern). Import `mkdir` from `'fs/promises'` and `join` from `'path'` — already imported at top of file.

    The tests WILL FAIL until Plan 03 completes (GlassProvider still returns null from registerRegion). That is expected behavior — they serve as the phase gate.
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `tests/glass-renderer.spec.ts` has a new `test.describe('GlassProvider integration', ...)` block at the end
    - `npx tsc --noEmit` exits 0 (TypeScript valid)
    - 4 new tests exist: non-black canvas, GlassPanel visible, resize smoke, screenshot save
    - Existing `GlassRenderer visual harness` block is unchanged
  </done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — must exit 0 (spec file is TypeScript-valid).
`npm run test:e2e` — run after Plan 03 completes as the phase gate. Tests in the new block will fail until GlassProvider is wired.
</verification>

<success_criteria>
- `tests/glass-renderer.spec.ts` contains `test.describe('GlassProvider integration', ...)` block
- 4 new tests covering: non-black rendering, GlassPanel presence, resize smoke, screenshot save
- TypeScript compiles without errors
- Existing harness tests unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-react-integration/17-02-SUMMARY.md` following the summary template.
</output>
