---
phase: 17-react-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/GlassRenderer.ts
  - src/renderer/__tests__/region-setters.test.ts
autonomous: true
requirements:
  - REACT-01
  - REACT-03

must_haves:
  truths:
    - "All 14 setRegionXxx methods exist on GlassRenderer and mutate region.target fields"
    - "setRegionMorphSpeed mutates region.morphSpeed (not region.target)"
    - "Calling a setter with an unknown id is a safe no-op (no throw)"
    - "All 14 setter unit tests pass with npm test"
  artifacts:
    - path: "src/renderer/GlassRenderer.ts"
      provides: "14 public setter methods"
      contains: "setRegionParams"
    - path: "src/renderer/__tests__/region-setters.test.ts"
      provides: "Unit tests for all 14 setters"
      exports: []
  key_links:
    - from: "src/renderer/__tests__/region-setters.test.ts"
      to: "src/renderer/GlassRenderer.ts"
      via: "direct import of GlassRenderer class"
      pattern: "import.*GlassRenderer"
---

<objective>
Add 14 typed setter methods to `GlassRenderer` so `GlassRegionHandle` closures (built in Plan 03) can route prop changes to `region.target` fields. Write unit tests first (TDD) to pin the contract.

Purpose: `GlassRegionHandle` has 17 methods — `remove()` and `updateRect()` are handled by the renderer itself; the remaining 14 map 1:1 to setter methods on `GlassRenderer`.
Output: 14 new public methods on `GlassRenderer`, 14 passing unit tests.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-react-integration/17-RESEARCH.md

@src/renderer/GlassRenderer.ts
@src/renderer/GlassRegionState.ts
@src/context/GlassContext.ts
@src/renderer/__tests__/uniforms.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing setter unit tests (RED)</name>
  <files>src/renderer/__tests__/region-setters.test.ts</files>
  <behavior>
    - Test: setRegionParams mutates target.cornerRadius, target.blurIntensity, target.opacity, target.refractionStrength
    - Test: setRegionTint mutates target.tint.r, target.tint.g, target.tint.b
    - Test: setRegionAberration mutates target.aberration
    - Test: setRegionSpecular mutates target.specularIntensity
    - Test: setRegionRim mutates target.rimIntensity
    - Test: setRegionMode mutates target.mode
    - Test: setRegionMorphSpeed mutates region.morphSpeed (GlassRegionState field, NOT GlassUniforms)
    - Test: setRegionContrast mutates target.contrast
    - Test: setRegionSaturation mutates target.saturation
    - Test: setRegionBlurRadius mutates target.blurRadius
    - Test: setRegionFresnelIOR mutates target.fresnelIOR
    - Test: setRegionFresnelExponent mutates target.fresnelExponent
    - Test: setRegionEnvReflectionStrength mutates target.envReflectionStrength
    - Test: setRegionGlareAngle mutates target.glareAngle
    - Test: calling any setter with unknown id does NOT throw
    - Test: setters do NOT mutate region.current (only target)
  </behavior>
  <action>
    Create `src/renderer/__tests__/region-setters.test.ts`.

    The test file must NOT use a real GPUDevice. Use a stub HTMLElement: `{ getBoundingClientRect: () => ({ left: 0, top: 0, width: 100, height: 50 }) } as unknown as HTMLElement`.

    Since GlassRenderer.init() requires a real GPU, test the setter logic by:
    1. Creating a GlassRenderer instance WITHOUT calling init()
    2. Manually inserting a GlassRegionState into `renderer['regions']` (private map access via bracket notation is allowed in tests)
    3. Calling the setter methods
    4. Asserting the target field changed

    Pattern for accessing private map (standard in this project — see uniforms.test.ts for similar direct-access style):
    ```typescript
    const renderer = new GlassRenderer();
    const el = { getBoundingClientRect: () => ({ left: 0, top: 0, width: 100, height: 50 }) } as unknown as HTMLElement;
    const id = 1;
    // Manually seed the private map
    (renderer as any)['regions'].set(id, {
      id,
      element: el,
      current: { ...DEFAULT_GLASS_UNIFORMS, tint: {...}, rect: {...}, resolution: {...} },
      target: { ...DEFAULT_GLASS_UNIFORMS, tint: {...}, rect: {...}, resolution: {...} },
      morphSpeed: 8,
    });
    ```

    Import `DEFAULT_GLASS_UNIFORMS` from `../GlassRegionState` for baseline values.

    These tests MUST fail on first run (methods don't exist yet). Confirm failure before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npm test -- --reporter=verbose src/renderer/__tests__/region-setters.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists; all setter tests fail with "is not a function" or equivalent — RED phase confirmed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 14 setter methods to GlassRenderer (GREEN)</name>
  <files>src/renderer/GlassRenderer.ts</files>
  <behavior>
    - setRegionParams(id, cornerRadius, blur, opacity, refraction): mutates target
    - setRegionTint(id, r, g, b): mutates target.tint as object { r, g, b }
    - setRegionAberration(id, v): target.aberration = v
    - setRegionSpecular(id, v): target.specularIntensity = v
    - setRegionRim(id, v): target.rimIntensity = v
    - setRegionMode(id, v): target.mode = v
    - setRegionMorphSpeed(id, speed): region.morphSpeed = speed (NOT target field)
    - setRegionContrast(id, v): target.contrast = v
    - setRegionSaturation(id, v): target.saturation = v
    - setRegionBlurRadius(id, v): target.blurRadius = v
    - setRegionFresnelIOR(id, v): target.fresnelIOR = v
    - setRegionFresnelExponent(id, v): target.fresnelExponent = v
    - setRegionEnvReflectionStrength(id, v): target.envReflectionStrength = v
    - setRegionGlareAngle(id, v): target.glareAngle = v
  </behavior>
  <action>
    Add the 14 methods to `GlassRenderer` class in `src/renderer/GlassRenderer.ts`. Insert them after the `getRegion()` method (line ~197) and before `render()`.

    Implementation style rules (from research):
    - Each setter is a public method, explicitly typed — no generic dispatcher
    - Guard pattern: `const region = this.regions.get(id); if (!region) return;`
    - Single-line setters (except setRegionParams and setRegionTint) using shorthand:
      `setRegionAberration(id: number, v: number): void { const r = this.regions.get(id); if (!r) return; r.target.aberration = v; }`
    - setRegionTint: `r.target.tint = { r, g, b }` (replace object, not mutate in-place)
    - setRegionMorphSpeed: `r.morphSpeed = speed` — morphSpeed is on GlassRegionState, NOT GlassUniforms
    - setRegionParams is multi-line: assigns all 4 fields on region.target individually

    CRITICAL: Do NOT mutate region.current. Only region.target. The render() loop's morphLerp() reads target and smoothly interpolates current toward it.

    After adding methods, run tests — all 14+ setter tests must pass (GREEN).

    Also run: `npx tsc --noEmit` — must have zero type errors.
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npm test -- --reporter=verbose src/renderer/__tests__/region-setters.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All setter tests pass; tsc --noEmit exits 0; GlassRenderer.ts has 14 new public methods after getRegion()</done>
</task>

</tasks>

<verification>
Run full unit suite to confirm no regressions: `npm test`
Run type-check: `npx tsc --noEmit`
Both must exit 0.
</verification>

<success_criteria>
- `src/renderer/__tests__/region-setters.test.ts` exists with 14+ test cases
- All tests green: `npm test` exits 0
- `GlassRenderer` has methods: setRegionParams, setRegionTint, setRegionAberration, setRegionSpecular, setRegionRim, setRegionMode, setRegionMorphSpeed, setRegionContrast, setRegionSaturation, setRegionBlurRadius, setRegionFresnelIOR, setRegionFresnelExponent, setRegionEnvReflectionStrength, setRegionGlareAngle
- Setters only mutate region.target (never region.current)
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-react-integration/17-01-SUMMARY.md` following the summary template.
</output>
