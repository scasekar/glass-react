---
phase: 19-tuning-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/controls/tokens.ts
  - demo/controls/SliderControl.tsx
  - demo/controls/ColorControl.tsx
  - demo/controls/SelectControl.tsx
  - demo/index.html
autonomous: true
requirements: [PAGE-01, PAGE-02]

must_haves:
  truths:
    - "All slider inputs have polished cross-browser thumb and track styling (no browser-default appearance)"
    - "Slider track shows a filled left-portion gradient reflecting the current value"
    - "Value readouts use monospace font with contextually correct decimal places (0 for integers, 2 for 0-1, 1 for angles)"
    - "Color channel rows show R/G/B channel labels and per-channel value readouts"
    - "SelectControl dropdown has a custom chevron arrow and no native appearance"
    - "No magic rgba() literals exist in SliderControl, ColorControl, or SelectControl — all colors come from tokens"
  artifacts:
    - path: "demo/controls/tokens.ts"
      provides: "Design token constants (color, space, radius, font, transition)"
      exports: ["tokens"]
    - path: "demo/controls/SliderControl.tsx"
      provides: "Polished slider with track fill and monospace value readout"
    - path: "demo/controls/ColorControl.tsx"
      provides: "Three-channel RGB slider control"
    - path: "demo/controls/SelectControl.tsx"
      provides: "Styled dropdown with custom chevron"
    - path: "demo/index.html"
      provides: "Global CSS for range input thumb/track via pseudo-elements"
      contains: "::-webkit-slider-thumb"
  key_links:
    - from: "demo/controls/SliderControl.tsx"
      to: "demo/controls/tokens.ts"
      via: "import { tokens }"
      pattern: "tokens\\.color\\."
    - from: "demo/index.html"
      to: "SliderControl input[type=range]"
      via: "global CSS pseudo-element rules"
      pattern: "webkit-slider-thumb"
---

<objective>
Create the design token foundation and rewrite all three primitive control components (SliderControl, ColorControl, SelectControl) with polished visual styles. Inject global CSS for range input pseudo-element styling into demo/index.html.

Purpose: Establish the typed token system and polished component primitives that Plan 02 (ControlPanel) will assemble. The quality bar is a macOS-native dev inspector panel — Xcode Attributes Inspector / Figma right panel aesthetic.
Output: tokens.ts, rewritten SliderControl/ColorControl/SelectControl, and CSS-augmented index.html.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-tuning-page-redesign/19-RESEARCH.md

<interfaces>
<!-- Current SliderControl props (from demo/controls/SliderControl.tsx) -->
```typescript
interface SliderControlProps {
  label: string;
  value: number;
  min: number;
  max: number;
  step: number;
  onChange: (value: number) => void;
}
```

<!-- Current ColorControl props (from demo/controls/ColorControl.tsx) -->
```typescript
import type { GlassColor } from '../../src/components/types';
interface ColorControlProps {
  label: string;
  value: GlassColor;  // [r, g, b] floats 0-1
  onChange: (value: GlassColor) => void;
}
```

<!-- Current SelectControl props (from demo/controls/SelectControl.tsx) -->
```typescript
interface SelectOption { value: string; label: string; }
interface SelectControlProps {
  label: string;
  value: string;
  options: SelectOption[];
  onChange: (value: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tokens.ts and inject global CSS into index.html</name>
  <files>demo/controls/tokens.ts, demo/index.html</files>
  <action>
Create `demo/controls/tokens.ts` with the exact token object from the research doc (Pattern 1). Export as `export const tokens = { ... } as const`. Include: `color` (panelBg, panelBorder, sectionBorder, trackBg, trackFill #5ba3f5, thumbBg, thumbShadow, labelPrimary, labelMuted, valueText, sectionTitle, presetChipBg, presetChipBgActive, presetChipBorder, presetChipBorderActive, errorText, accentBlue), `space` (panelPad, sectionGap, controlGap, chipGap), `radius` (panel:0, chip:5, control:6, thumb:'50%'), `font` (sectionTitle, label, value, chip, header), `transition` (fast:'all 0.12s ease', panel:'transform 0.22s cubic-bezier(0.4,0,0.2,1)').

Update `demo/index.html`: Inside the existing `<style>` block (after `#root { min-height: 100vh; }`), add the range input global CSS from the research doc (Patterns 2 and 3). Specifically:
- `input[type="range"]` reset: `-webkit-appearance: none; appearance: none; width: 100%; height: 3px; background: transparent; outline: none; cursor: pointer;`
- `::-webkit-slider-runnable-track`: height 3px, background uses `linear-gradient(to right, #5ba3f5 var(--pct,0%), rgba(255,255,255,0.12) var(--pct,0%))` — the two-stop gradient for fill effect
- `::-webkit-slider-thumb`: 13px circle, white, box-shadow `0 1px 4px rgba(0,0,0,0.6)`, margin-top -5px, transition for hover
- `:active::-webkit-slider-thumb`: scale(1.2) and accent blue glow `box-shadow: 0 2px 8px rgba(91,163,245,0.5)`
- `::-moz-range-track` and `::-moz-range-thumb` equivalents (flat track for Firefox — acceptable for a WebGPU dev tool)
- Also add a polished `select` reset for SelectControl: `select { color-scheme: dark; }` to prevent white flash on option list
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && grep -c "tokens" demo/controls/tokens.ts && grep -c "webkit-slider-thumb" demo/index.html</automated>
  </verify>
  <done>tokens.ts exports a `tokens` const with all sub-objects. index.html contains range pseudo-element CSS rules.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SliderControl, ColorControl, and SelectControl with token styles</name>
  <files>demo/controls/SliderControl.tsx, demo/controls/ColorControl.tsx, demo/controls/SelectControl.tsx</files>
  <action>
**SliderControl.tsx** — complete rewrite using tokens. Keep props interface identical. Changes:
- Import `{ tokens }` from `./tokens`
- Add `formatValue(value, step, max)`: returns `value.toFixed(0)` if step >= 1; `value.toFixed(2)` if max <= 1; else `value.toFixed(1)`
- Compute `const pct = ((value - min) / (max - min)) * 100`
- Render label row: label left in `tokens.font.label` / `tokens.color.labelPrimary`, value readout right in monospace `tokens.font.value` / `tokens.color.valueText`, minWidth 38px, textAlign right
- `<input type="range">` sets `style={{ '--pct': `${pct}%` } as React.CSSProperties}` plus `width:'100%'`. Remove all old inline `appearance`, `background`, `accentColor` styles (those are now handled by global CSS). Keep `outline:'none'` and `cursor:'pointer'` if needed.
- Wrapper div: `marginBottom: tokens.space.controlGap` (10px)

**ColorControl.tsx** — complete rewrite using tokens. Keep props interface identical. Changes:
- Import `{ tokens }` from `./tokens`
- Remove inline `channelStyle` object with `accentColor` (global CSS handles it now)
- Channel row: label letter (R/G/B) in their existing colors `#f66`/`#6f6`/`#66f`, width 12px, then `<input type="range">` with `{ '--pct': ... } as React.CSSProperties`, then value readout in `tokens.color.valueText` / `tokens.font.value` / monospace, width 28px
- Preview swatch: `borderRadius: tokens.radius.control` (6px), `border: '1px solid rgba(255,255,255,0.15)'`
- Wrapper: `marginBottom: tokens.space.controlGap`

**SelectControl.tsx** — complete rewrite using tokens. Keep props interface identical. Changes:
- Import `{ tokens }` from `./tokens`
- Label uses `tokens.color.labelPrimary`, `tokens.font.label`
- Select uses `tokens.color.labelPrimary` text, `background: tokens.color.presetChipBg` (same dark frosted look), `border: '1px solid rgba(255,255,255,0.15)'`, `borderRadius: tokens.radius.control`, padding `6px 28px 6px 8px`, fontSize `tokens.font.label`
- Keep the SVG chevron backgroundImage for the dropdown arrow (same as current — it works well)
- Add `transition: tokens.transition.fast` on focus state via inline `onFocus`/`onBlur` state or simply leave it — focus ring from browser is acceptable for a dev tool
- Wrapper: `marginBottom: tokens.space.controlGap`

**TypeScript note for all files:** When setting `style={{ '--pct': ... }}`, cast as `React.CSSProperties`. This is the documented workaround for CSS custom properties in React JSX.
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "demo/controls/(Slider|Color|Select)" | head -20 || echo "No errors in control files"</automated>
  </verify>
  <done>SliderControl, ColorControl, SelectControl compile without TypeScript errors. No `accentColor` or magic rgba literals remain in these three files. All color/size values reference tokens.</done>
</task>

</tasks>

<verification>
1. `grep -r "accentColor" demo/controls/` returns no results (removed from all components)
2. `grep -r "rgba(" demo/controls/SliderControl.tsx demo/controls/ColorControl.tsx demo/controls/SelectControl.tsx` returns only the R/G/B channel color labels — no magic panel colors
3. `demo/controls/tokens.ts` exists with `export const tokens`
4. `demo/index.html` contains `::-webkit-slider-thumb` CSS rule
5. TypeScript compilation passes for demo/controls files
</verification>

<success_criteria>
- tokens.ts exports typed token object with all color/space/radius/font/transition values
- index.html has global range input pseudo-element CSS (thumb, track, fill gradient)
- SliderControl shows contextual decimal formatting and --pct CSS custom property for track fill
- ColorControl has per-channel RGB sliders styled with tokens
- SelectControl has styled dropdown using tokens
- Zero TypeScript errors in the three rewritten files
</success_criteria>

<output>
After completion, create `/Users/asekar/code/glass-react/.planning/phases/19-tuning-page-redesign/19-01-SUMMARY.md`
</output>
