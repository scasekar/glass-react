---
phase: 15-wasm-thinning
plan: "03"
type: execute
wave: 2
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - engine/build-web/engine.js
autonomous: false
requirements:
  - DEV-01
  - DEV-02
  - DEV-03
  - DEV-04
  - DEV-05

must_haves:
  truths:
    - "WASM binary builds from thinned C++ source without errors"
    - "Dev server starts and loads without console errors"
    - "Background (noise or image) renders on the canvas — visible to human eye"
    - "No glass overlay appears (glass pipeline is gone)"
    - "WASM binary size is smaller than v2.0 baseline (1,539,156 bytes)"
    - "JS console shows BackgroundEngine initialized (external device)"
  artifacts:
    - path: "engine/build-web/engine.js"
      provides: "Rebuilt thinned WASM — no glass shader, smaller binary"
  key_links:
    - from: "GlassProvider JS rAF loop"
      to: "C++ renderBackground()"
      via: "engine.renderBackground() called each frame"
      pattern: "renderBackground"
    - from: "JS getSceneTexture()"
      to: "C++ getSceneTextureHandleJS()"
      via: "module.getSceneTextureHandle() → WebGPU.getJsObject(handle)"
      pattern: "getSceneTextureHandle"
---

<objective>
Build the thinned WASM binary from the edited C++ source (Plan 01), then run the dev server and perform a human visual verification that background renders correctly without the glass pipeline. Also confirm scene texture is accessible from JS.

Purpose: Integration gate — confirms Plans 01 and 02 work together end-to-end.
Output: Rebuilt engine.js (smaller), visual confirmation, phase sign-off.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-wasm-thinning/15-01-SUMMARY.md
@.planning/phases/15-wasm-thinning/15-02-SUMMARY.md
@.planning/ROADMAP.md

v2.0 WASM baseline size: 1,539,156 bytes. Post-thinning binary must be strictly smaller.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build thinned WASM and verify binary size reduction</name>
  <files>engine/build-web/engine.js</files>
  <action>
Run the WASM build from scratch:

```bash
cd /Users/asekar/code/glass-react/engine/build-web && emcmake cmake .. -DCMAKE_BUILD_TYPE=Release 2>&1 | tail -10 && emmake make -j$(nproc) 2>&1 | tail -30
```

If build fails, read the full error output. Common fixes:
- `surfaceFormat` still referenced → grep background_engine.cpp, replace with kOffscreenFormat
- Deleted method still called → check main.cpp or background_engine.cpp for stale references
- Missing `renderBackground` implementation → check background_engine.cpp

After successful build, check binary size:
```bash
wc -c /Users/asekar/code/glass-react/engine/build-web/engine.js
```

Expected: smaller than 1,539,156 bytes (v2.0 baseline). The glass.wgsl.h shader is large — expect a meaningful reduction (several hundred KB).

Also verify the new Embind exports are present in the built JS:
```bash
grep -o "getSceneTextureHandle\|renderBackground\|initWithExternalDevice" /Users/asekar/code/glass-react/engine/build-web/engine.js | sort | uniq
```
  </action>
  <verify>
    <automated>wc -c /Users/asekar/code/glass-react/engine/build-web/engine.js 2>/dev/null | awk '$1 < 1539156 {print "PASS: binary smaller than v2.0 baseline"} $1 >= 1539156 {print "FAIL: binary NOT smaller than v2.0 baseline"}'</automated>
  </verify>
  <done>
  - WASM build exits 0
  - Binary size less than 1,539,156 bytes
  - `getSceneTextureHandle`, `renderBackground`, and `initWithExternalDevice` all appear in the built engine.js
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — background renders, no glass overlay, no WebGPU errors</name>
  <files>none</files>
  <action>Human verifies the running dev app. Start `npm run dev` and inspect browser + console.</action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify below"</automated>
  </verify>
  <done>Human approves: background renders, no WebGPU errors, no glass overlay visible.</done>
  <what-built>
Thinned WASM engine (Plan 01) + updated TypeScript loader and GlassProvider (Plan 02). The app now:
- Has JS create the GPUDevice via navigator.gpu
- Passes the device to the thinned C++ engine via initWithExternalDevice
- Drives rendering via JS requestAnimationFrame calling engine.renderBackground()
- C++ renders noise or image background to offscreen RGBA8Unorm texture only (no glass pass)
- Glass region components return null handles (TODO Phase 17)
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev` in /Users/asekar/code/glass-react/ and open the dev app (http://localhost:5173 or similar)

2. EXPECT: The canvas shows the background (noise gradient or wallpaper image) filling the full screen.

3. EXPECT: NO glass overlay — no refraction, blur, or tinted glass panels visible. The content renders directly over the background with no glass effect.

4. EXPECT: No red errors in the browser console. Check specifically for:
   - WebGPU device errors (format mismatch, pipeline errors)
   - "engine is null" or "getEngine() null" errors
   - Uncaught exceptions from the rAF loop

5. Look for console.log output: "BackgroundEngine initialized (external device)" — this confirms C++ received the JS-created device and initialized successfully (DEV-01, DEV-02).

6. The dev/tuning page may show glass UI elements with no glass effect rendered (expected — glass pass is gone, Phase 17 will restore it). The background should be visible behind them.

7. Resize the browser window — background should resize without console errors or crashes.
  </how-to-verify>
  <resume-signal>Type "approved" if background renders and no WebGPU errors appear. Describe any issues if something is wrong.</resume-signal>
</task>

</tasks>

<verification>
Phase 15 gate — ALL must be true:

1. WASM binary smaller than 1,539,156 bytes:
   `wc -c engine/build-web/engine.js`

2. No emscripten_set_main_loop in C++ source:
   `grep -r "emscripten_set_main_loop" engine/src/` → no output

3. No glass code in C++ source:
   `grep -r "GlassUniforms\|glassPipeline\|glass.wgsl\|addGlassRegion" engine/src/` → no output

4. TypeScript compiles:
   `cd /Users/asekar/code/glass-react && npx tsc --noEmit` → exits 0

5. Human confirms: background renders, no WebGPU errors, no glass overlay
</verification>

<success_criteria>
- WASM binary built from thinned C++ source — size < 1,539,156 bytes
- Dev app renders background (noise/image) in canvas without glass overlay
- No WebGPU validation errors in browser console
- C++ engine logs "BackgroundEngine initialized (external device)" — confirms DEV-01 and DEV-02
- Phase 15 requirements DEV-01 through DEV-05 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/15-wasm-thinning/15-03-SUMMARY.md` with:
- Final WASM binary size (before and after)
- Human verification result (approved / issues found)
- Any fixes required between Plan 01/02 execution and this plan's build
- Phase 15 sign-off status

Also update `.planning/STATE.md`:
- status: in_progress → completed (if all passes)
- last_activity: Phase 15 complete — C++ thinned, JS rAF loop, external device path only
- Phase 15 row in progress table: 3/3

Update `.planning/ROADMAP.md` progress table:
- Phase 15 status: Not started → Complete
- Plans Complete: 0/? → 3/3
</output>
