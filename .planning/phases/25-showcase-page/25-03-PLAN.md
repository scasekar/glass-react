---
phase: 25-showcase-page
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - src/showcase/sections/DeveloperSection.tsx
  - src/showcase/ShowcasePage.tsx
autonomous: false
requirements: [SHOW-01, SHOW-04]

must_haves:
  truths:
    - "Developer section shows npm install command with a copy button"
    - "Developer section shows a minimal code example with GlassProvider + one control"
    - "Developer section shows browser compatibility info"
    - "The complete showcase page looks like a professional product landing page"
    - "Switching wallpaper between image and noise mode shows all controls rendering correctly in both"
  artifacts:
    - path: "src/showcase/sections/DeveloperSection.tsx"
      provides: "Quick-start section with install command, code example, browser compat"
      exports: ["DeveloperSection"]
  key_links:
    - from: "src/showcase/ShowcasePage.tsx"
      to: "src/showcase/sections/DeveloperSection.tsx"
      via: "wrapped in VirtualSection id=developer"
      pattern: "<VirtualSection.*developer"
---

<objective>
Build the developer quick-start section, then verify the complete showcase page visually via human checkpoint.

Purpose: Complete the showcase with the conversion-focused developer section and verify the full page meets professional quality standards.
Output: Complete showcase page ready for demonstration.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-showcase-page/25-RESEARCH.md
@.planning/phases/25-showcase-page/25-01-SUMMARY.md
@.planning/phases/25-showcase-page/25-02-SUMMARY.md

@src/tokens/apple.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Developer quick-start section + final wiring</name>
  <files>
    src/showcase/sections/DeveloperSection.tsx,
    src/showcase/ShowcasePage.tsx
  </files>
  <action>
    **DeveloperSection.tsx:**
    - Props: none
    - Narrative: "Start in 5 Minutes" -- the conversion section that turns evaluators into adopters
    - Layout: Section title + two side-by-side cards (flex row on desktop, column on mobile via flex-wrap)

    Card 1 -- "Install" (GlassPanel, cornerRadius APPLE_RADII.lg, padding 32px, flex:1, min-width 300px):
    - `<h3>` "Quick Start"
    - Install command block: `<pre>` styled as a code block with dark background (`rgba(0,0,0,0.3)`, border-radius 12px, padding 16px, font-family monospace)
    - Command text: `npm install liquidglass-react`
    - Copy button: A small `<GlassButton>` or plain `<button>` with clipboard icon positioned at top-right of the pre block
    - onClick: `navigator.clipboard.writeText('npm install liquidglass-react')` wrapped in try/catch
    - After copy, briefly change button text/icon to a checkmark (local state `copied`, reset after 2s via setTimeout)

    Card 2 -- "Usage" (GlassPanel, cornerRadius APPLE_RADII.lg, padding 32px, flex:1, min-width 300px):
    - `<h3>` "Minimal Example"
    - Code block showing:
      ```
      import { GlassProvider, GlassButton } from 'liquidglass-react';

      function App() {
        return (
          <GlassProvider>
            <GlassButton onClick={() => alert('Hello!')}>
              Click Me
            </GlassButton>
          </GlassProvider>
        );
      }
      ```
    - Styled same as install code block (dark bg, monospace, 0.85rem)
    - Syntax highlighting NOT needed -- plain white text in pre/code is fine

    Below the cards:
    - Browser compatibility line: `<p>` "WebGPU: Chrome 113+, Edge 113+, Safari 18+" -- muted text (60% opacity), centered
    - GitHub link: `<GlassButton>` "View on GitHub" -- centered, opens github.com in new tab (or # for now)
    - Use APPLE_SPACING for all gaps and padding

    **ShowcasePage.tsx update:**
    - Replace the developer placeholder with `<DeveloperSection />`
    - Import from `./sections/DeveloperSection`
    - Ensure it's wrapped in the existing `<VirtualSection id="developer" minHeight={300}>`
    - Add a simple footer below all sections: `<footer>` with "Built with WebGPU" text, muted, centered, padding 60px 0

    **Final visual polish pass on ShowcasePage:**
    - Verify the sticky header has `backdropFilter: 'blur(20px)'` or uses a GlassPanel for glass effect
    - Ensure all section titles share the same typography: 2rem, bold 700, centered, white
    - Ensure all section subtitles: 1rem, normal weight, centered, 60% opacity
    - Add `scrollBehavior: 'smooth'` to the html/root element style if not already present (or rely on scrollIntoView behavior:'smooth')
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Developer section shows install command with copy button, minimal code example, browser compatibility, and GitHub link. Full showcase page is complete with all six sections. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete showcase page</name>
  <files>none</files>
  <action>
    Human visually verifies the complete showcase page. Before presenting to the user, run `npm run dev` and take a Playwright screenshot to confirm the page renders without errors. Then present the verification steps to the user.
  </action>
  <what-built>
    Complete showcase page replacing the old demo as the app's main entry point. Includes:
    - Sticky glass header with navigation and tuning drawer toggle
    - Hero section with headline, wallpaper selector, and CTA buttons
    - Interactive controls section (settings card with toggle, slider, segmented control)
    - Navigation section (mock app frame with nav bar and toolbar)
    - Overlay section (alert and action sheet triggers)
    - Form section (chips, stepper, input in a preferences form)
    - Developer section (install command, code example, browser compat)
    - TuningDrawer slide-in panel
    - VirtualSection GPU region management
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. Verify the page loads as a product landing page (NOT the old panel/button demo)
    3. Scroll through all sections -- each should have contextual framing, not component labels
    4. Click the wallpaper selector in the hero -- switch between Wallpaper and Noise modes. All glass controls should update live.
    5. Click the gear icon in the sticky header -- TuningDrawer should slide in from the right
    6. Close the TuningDrawer -- slider values should be preserved (not reset)
    7. Scroll to bottom -- Developer section should show install command with copy button
    8. Click the copy button -- verify clipboard has "npm install liquidglass-react"
    9. Overall impression: Does this look like a product landing page or a component kitchen sink?
  </how-to-verify>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves the showcase page as a professional product landing page. All interactive features work correctly.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All six showcase sections render with narrative framing
- Wallpaper selector switches backgrounds and all glass updates
- TuningDrawer opens/closes smoothly
- Copy button works for install command
- Visual quality meets "professional product landing page" standard
</verification>

<success_criteria>
- Complete showcase page that a designer would recognize as a product page, not a Storybook
- Wallpaper switching works in both modes (SHOW-04)
- Developer section provides clear "how to start" path (SHOW-01)
- Human approves the overall visual quality and page narrative
</success_criteria>

<output>
After completion, create `.planning/phases/25-showcase-page/25-03-SUMMARY.md`
</output>
