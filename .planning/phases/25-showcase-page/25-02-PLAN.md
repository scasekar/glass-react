---
phase: 25-showcase-page
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - src/showcase/sections/HeroSection.tsx
  - src/showcase/sections/InteractiveSection.tsx
  - src/showcase/sections/NavigationSection.tsx
  - src/showcase/sections/OverlaySection.tsx
  - src/showcase/sections/FormSection.tsx
  - src/showcase/ShowcasePage.tsx
autonomous: true
requirements: [SHOW-01, SHOW-03, SHOW-04]

must_haves:
  truths:
    - "Hero section fills the viewport with headline, tagline, and CTA buttons"
    - "Interactive section demonstrates toggle, slider, and segmented control in a settings card context"
    - "Navigation section shows nav bar and toolbar in a mock app frame"
    - "Overlay section shows alert and action sheet triggers with working overlays"
    - "Form section shows chip, stepper, and input in a mock form context"
    - "Wallpaper selector allows switching between image and noise backgrounds"
    - "Scrolling through all sections does not visually break or show missing glass effects"
  artifacts:
    - path: "src/showcase/sections/HeroSection.tsx"
      provides: "Full-viewport hero with headline, tagline, wallpaper selector, CTA"
      exports: ["HeroSection"]
    - path: "src/showcase/sections/InteractiveSection.tsx"
      provides: "Settings card mockup with toggle, slider, segmented control"
      exports: ["InteractiveSection"]
    - path: "src/showcase/sections/NavigationSection.tsx"
      provides: "Mock app frame with navigation bar and toolbar"
      exports: ["NavigationSection"]
    - path: "src/showcase/sections/OverlaySection.tsx"
      provides: "Overlay trigger buttons with alert and action sheet demos"
      exports: ["OverlaySection"]
    - path: "src/showcase/sections/FormSection.tsx"
      provides: "Mock form with chip group, stepper, and input"
      exports: ["FormSection"]
  key_links:
    - from: "src/showcase/ShowcasePage.tsx"
      to: "src/showcase/sections/HeroSection.tsx"
      via: "direct import and render (not virtualized)"
      pattern: "<HeroSection"
    - from: "src/showcase/ShowcasePage.tsx"
      to: "src/showcase/sections/InteractiveSection.tsx"
      via: "wrapped in VirtualSection"
      pattern: "<VirtualSection.*interactive"
    - from: "src/showcase/sections/HeroSection.tsx"
      to: "GlassSegmentedControl"
      via: "wallpaper selector using segments for image/noise"
      pattern: "GlassSegmentedControl"
---

<objective>
Build all showcase content sections: hero with wallpaper selector, interactive controls demo, navigation mockup, overlay triggers, and form demo. Wire sections into ShowcasePage replacing placeholders.

Purpose: Transform the showcase shell into a professional product landing page that demonstrates all available glass controls in contextual scenarios (not a component grid).
Output: Five content sections with narrative-driven control demonstrations and a working wallpaper selector.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-showcase-page/25-RESEARCH.md
@.planning/phases/25-showcase-page/25-01-SUMMARY.md

@src/tokens/apple.ts
@src/components/controls/types.ts
@src/components/controls/index.ts
@src/index.ts

<interfaces>
<!-- Controls available for showcase sections -->

From src/components/controls/index.ts (Phase 21-22):
```typescript
export { GlassToggle } from './GlassToggle';      // { checked, onCheckedChange, label }
export { GlassSlider } from './GlassSlider';       // { value, onValueChange, min, max, step, label }
export { GlassSegmentedControl } from './GlassSegmentedControl'; // { value, onValueChange, segments: [{value, label}] }
export { GlassChip } from './GlassChip';           // selectable pill
export { GlassStepper } from './GlassStepper';     // +/- increment
export { GlassInput } from './GlassInput';         // text field with glass border
```

From src/components (primitives):
```typescript
export { GlassPanel } from './GlassPanel';   // Glass surface container (div)
export { GlassButton } from './GlassButton'; // Glass button with onClick
export { GlassCard } from './GlassCard';     // Glass article container
```

Navigation controls (Phase 23-01, on disk but may not be in barrel exports):
```typescript
// src/components/controls/GlassNavigationBar.tsx
export { GlassNavigationBar }; // { title, onBack?, actions? }
// src/components/controls/GlassToolbar.tsx
export { GlassToolbar };       // { actions: GlassToolbarAction[] }
```

Overlay controls (Phase 24-01, on disk but may not be in barrel exports):
```typescript
// src/components/controls/GlassAlert.tsx
export { GlassAlert };         // { open, onOpenChange, title, message?, actions }
// src/components/controls/GlassActionSheet.tsx
export { GlassActionSheet };   // { open, onOpenChange, title?, actions, cancelLabel? }
```

From src/components/GlassProvider.tsx:
```typescript
export interface GlassProviderProps {
  backgroundMode?: 'image' | 'noise';
}
```

From src/tokens/apple.ts:
```typescript
export const APPLE_SPACING: { xs: 4, sm: 8, md: 16, lg: 20, xl: 24, xxl: 32, xxxl: 48 };
export const APPLE_RADII: { sm: 8, md: 14, lg: 20, xl: 28, pill: 9999 };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero, Interactive, and Form sections</name>
  <files>
    src/showcase/sections/HeroSection.tsx,
    src/showcase/sections/InteractiveSection.tsx,
    src/showcase/sections/FormSection.tsx
  </files>
  <action>
    Create `src/showcase/sections/` directory.

    **HeroSection.tsx:**
    - Props: `{ backgroundMode: 'image' | 'noise'; onBackgroundModeChange: (mode: 'image' | 'noise') => void }`
    - Full viewport height: `minHeight: '100vh'`, flex column, center content vertically
    - Content (centered, max-width 800px):
      - Headline: `<h1>` "Liquid Glass for React" -- large (3rem), bold (700), system font, white
      - Tagline: `<p>` "WebGPU-powered refraction and specular lighting. Not CSS blur." -- 1.2rem, rgba(255,255,255,0.7)
      - Wallpaper selector: `<GlassSegmentedControl>` with segments `[{value:'image', label:'Wallpaper'}, {value:'noise', label:'Noise'}]`, value={backgroundMode}, onValueChange={onBackgroundModeChange}. Place below the tagline with 32px gap.
      - CTA row (flex, gap 16px, margin-top 40px):
        - Primary: `<GlassButton refractionMode="prominent" cornerRadius={APPLE_RADII.md}>` "View on GitHub" -- onClick opens github in new tab (or logs for now)
        - Secondary: `<GlassButton cornerRadius={APPLE_RADII.md}>` "Explore Controls" -- onClick scrolls to #interactive via `scrollIntoView({ behavior: 'smooth' })`
    - The hero IS the demo -- the glass CTA buttons and segmented control against the live wallpaper background ARE the visual proof
    - No `<section id="hero">` wrapper needed -- ShowcasePage already provides it (or add id="hero" if not)
    - IMPORTANT: Do NOT show prop names, variant labels, or anything that looks like documentation

    **InteractiveSection.tsx:**
    - Props: none (self-contained state)
    - Narrative: "Controls that feel alive" -- these controls have physics-based spring animations and real GPU glass
    - Layout: Section title `<h2>` "Controls That Feel Alive" + subtitle, then a mock "Settings" card
    - The "Settings" card is a `<GlassPanel cornerRadius={APPLE_RADII.xl}>` styled as a settings panel (padding 32px, max-width 480px, centered)
    - Inside the card, three setting rows (flex column, gap 24px):
      1. Row: "Notifications" label (left) + `<GlassToggle>` (right) -- local state `checked`
      2. Row: "Volume" label (left) + `<GlassSlider min={0} max={100} step={1}>` (below) -- local state `volume`
      3. Row: "Appearance" label (above) + `<GlassSegmentedControl>` with segments [{value:'light', label:'Light'}, {value:'dark', label:'Dark'}, {value:'auto', label:'Auto'}] -- local state `appearance`
    - Each row has a realistic label -- NO prop documentation, NO "checked={true}" labels
    - Section title styling: 2rem, bold, centered, with a subtle tagline below (1rem, 60% opacity)
    - Use APPLE_SPACING for all padding/gaps

    **FormSection.tsx:**
    - Props: none (self-contained state)
    - Narrative: "The Complete Control Palette" -- chips, steppers, inputs for any form
    - Layout: Section title + a mock "Preferences" form card
    - The form card is a `<GlassPanel cornerRadius={APPLE_RADII.xl}>` (padding 32px, max-width 480px, centered)
    - Inside the card:
      1. "Interests" label + a row of `<GlassChip>` components: "Design", "Engineering", "Music", "Photography" -- local state tracks which are selected (multi-select with a Set or array)
      2. "Quantity" label + `<GlassStepper>` -- local state `quantity`, min 1, max 10
      3. "Email" label + `<GlassInput>` -- local state `email`, placeholder "you@example.com"
    - Realistic form context -- NOT a component catalog

    **Design quality rules for ALL sections:**
    - System font stack: `-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`
    - White text on transparent background (the glass canvas is behind everything)
    - Section vertical rhythm: each section has `paddingTop: 120` (120px breathing room per research)
    - Max-width 1200px container with `margin: 0 auto` and `padding: 0 24px` for mobile
    - No borders, no outlines, no visible containers except glass panels
    - Subtle text hierarchy: h2 at 2rem/700, body at 1rem/400, muted at 0.85rem/60% opacity
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Three sections render with contextual control demos. Hero has headline, tagline, wallpaper selector, and CTA buttons. Interactive section shows a settings card. Form section shows a preferences form. No prop names or documentation visible -- all controls are in realistic use contexts.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation and Overlay sections + wire into ShowcasePage</name>
  <files>
    src/showcase/sections/NavigationSection.tsx,
    src/showcase/sections/OverlaySection.tsx,
    src/showcase/ShowcasePage.tsx
  </files>
  <action>
    **NavigationSection.tsx:**
    - Props: none (self-contained state)
    - Narrative: "Real App Navigation" -- these aren't toy demos, they're production navigation patterns
    - Layout: Section title + a mock phone/app frame
    - The mock frame is a `<GlassPanel cornerRadius={APPLE_RADII.xl}>` styled as a phone-like container (max-width 400px, height ~500px, centered, overflow hidden, position relative)
    - Inside the frame:
      1. Top: `<GlassNavigationBar title="Photos" onBack={() => {}} actions={[{id:'share', icon: shareIcon, label:'Share', onPress: () => {}}]} />` -- import GlassNavigationBar directly from `../components/controls/GlassNavigationBar` if not in barrel
      2. Middle: Placeholder content area (some mock photo grid using simple colored divs or just placeholder text "Content Area" with muted styling)
      3. Bottom: `<GlassToolbar actions={[...3-4 icon actions...]} />` -- import GlassToolbar directly
    - For icons: Use simple unicode characters or inline SVG paths (no icon library needed): share = "\u{2191}" or a simple arrow SVG, heart = "\u{2661}", bookmark = "\u{2606}", more = "\u{2026}"
    - NOTE: GlassTabBar and GlassSearchBar are NOT yet built (Phase 23-02 pending). Do NOT import them. Use only GlassNavigationBar and GlassToolbar which exist from Phase 23-01.

    **OverlaySection.tsx:**
    - Props: none (self-contained state)
    - Narrative: "Fluid Overlays" -- modals and sheets with the same glass quality
    - Layout: Section title + trigger buttons row + overlay instances
    - Two trigger scenarios:
      1. "Show Alert" `<GlassButton>` -- opens a `<GlassAlert>` with title "Delete Photo?", message "This action cannot be undone.", actions: [{label:'Cancel', onPress: close, style:'default'}, {label:'Delete', onPress: close, style:'destructive'}]
      2. "Show Actions" `<GlassButton>` -- opens a `<GlassActionSheet>` with title "Share Photo", actions: [{label:'Copy Link', onPress: close}, {label:'Save to Files', onPress: close}, {label:'AirDrop', onPress: close}], cancelLabel: "Cancel"
    - Local state: `alertOpen: boolean`, `sheetOpen: boolean`
    - Import GlassAlert and GlassActionSheet directly from their files if not in barrel exports
    - NOTE: GlassSheet and GlassPopover are NOT yet built (Phase 24-02 pending). Do NOT import them. Use only GlassAlert and GlassActionSheet which exist from Phase 24-01.
    - The trigger buttons should be in a flex row with gap, centered

    **ShowcasePage.tsx update:**
    - Replace all placeholder section content with actual section components
    - Import all five sections from `./sections/`
    - HeroSection: Rendered directly (NOT inside VirtualSection) -- it's always visible above fold
      - Pass `backgroundMode` and `onBackgroundModeChange` props
    - InteractiveSection: Inside `<VirtualSection id="interactive" minHeight={500}>`
    - NavigationSection: Inside `<VirtualSection id="navigation" minHeight={600}>`
    - OverlaySection: Inside `<VirtualSection id="overlays" minHeight={400}>`
    - FormSection: Inside `<VirtualSection id="forms" minHeight={500}>`
    - DeveloperSection placeholder: Keep as `<VirtualSection id="developer" minHeight={300}>` with placeholder text "Developer Quick Start" (filled in Plan 03)
    - Ensure section order matches the IA from research: Hero > Interactive > Navigation > Overlays > Forms > Developer
    - Update sticky header nav links to match actual section IDs

    **Import resolution:** If GlassNavigationBar, GlassToolbar, GlassAlert, GlassActionSheet are not exported from the controls barrel (`src/components/controls/index.ts`), import them directly from their individual files (e.g., `import { GlassNavigationBar } from '../components/controls/GlassNavigationBar'`). Do NOT modify the barrel exports -- that belongs to their respective phases.
  </action>
  <verify>
    <automated>cd /Users/asekar/code/glass-react && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>All five content sections render in ShowcasePage. Navigation section shows a mock app frame with nav bar and toolbar. Overlay section has working alert and action sheet triggers. Sections are wrapped in VirtualSection (except hero). Build succeeds with no type errors.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- All sections render in browser with contextual demos (not component grids)
- Wallpaper selector in hero switches between image and noise backgrounds
- VirtualSection gates properly -- sections below fold mount only when scrolled near
- Alert and action sheet overlays open and close properly
- Navigation section shows nav bar and toolbar in a mock phone frame
</verification>

<success_criteria>
- Five content sections with narrative framing, not component catalogs
- Wallpaper selector (SHOW-04) switches backgroundMode and all glass updates live
- VirtualSection (SHOW-03) keeps GPU regions under budget during scrolling
- Each section answers one question per the IA contract from research
- No prop names, variant labels, or documentation-style content in the showcase
</success_criteria>

<output>
After completion, create `.planning/phases/25-showcase-page/25-02-SUMMARY.md`
</output>
