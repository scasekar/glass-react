---
phase: 18-visual-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/lib/capture-web.ts
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "WASM binary is built from current source before any capture runs"
    - "npm run diff produces a web screenshot showing the wallpaper background (not solid black)"
    - "Baseline diff scores are captured and printed to stdout for both light and dark presets"
    - "Capture settle time is sufficient: web screenshot shows circular glass button over wallpaper"
  artifacts:
    - path: "pipeline/output/web/light.png"
      provides: "v3.0 web capture for light preset"
    - path: "pipeline/output/web/dark.png"
      provides: "v3.0 web capture for dark preset"
    - path: "pipeline/output/diffs/light_diff.png"
      provides: "pixel diff image for light mode"
    - path: "pipeline/output/diffs/dark_diff.png"
      provides: "pixel diff image for dark mode"
    - path: "pipeline/output/reports/light_report.html"
      provides: "side-by-side HTML diff report"
  key_links:
    - from: "pipeline/lib/capture-web.ts"
      to: "#gpu-canvas"
      via: "page.locator('#gpu-canvas')"
      pattern: "gpu-canvas"
    - from: "npm run diff"
      to: "pipeline/output/"
      via: "pipeline/diff.ts"
      pattern: "npm run diff"
---

<objective>
Build the current WASM binary, run the full diff pipeline to establish v3.0 baseline scores against iOS reference, and verify the web captures show the wallpaper background correctly. Increase GPU settle time if the captured web screenshots show solid black instead of the wallpaper background.

Purpose: Establish whether the v3.0 JS glass pipeline has regressed from the v2.0 baseline (light: 0.40%, dark: 0.85%). This measurement drives whether Plan 02 needs to run tuning at all.

Output: Scored diff reports in pipeline/output/reports/ and captured web/iOS PNGs in pipeline/output/.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-visual-validation/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build current WASM binary</name>
  <files>engine/build-web/glass.js, engine/build-web/glass.wasm</files>
  <action>
    Kill any running dev server to avoid port conflicts, then rebuild the WASM engine from current source.

    Stop any dev server on port 5173:
    ```
    kill $(lsof -ti:5173) 2>/dev/null || true
    ```

    Build WASM from project root:
    ```
    npm run build:wasm
    ```

    Verify the build succeeded: `engine/build-web/glass.js` and `engine/build-web/glass.wasm` must exist with modification timestamps newer than any `engine/src/` source files.

    If `npm run build:wasm` fails, read the error and check the Emscripten CMake build log. Common cause: `engine/build-web/` directory may need to be created, or the Emscripten toolchain path is not in PATH. Do NOT proceed to Task 2 if the WASM build fails — the captured screenshots would use a stale binary.
  </action>
  <verify>
    <automated>ls -lt /Users/asekar/code/glass-react/engine/build-web/glass.js /Users/asekar/code/glass-react/engine/build-web/glass.wasm | head -3</automated>
  </verify>
  <done>glass.js and glass.wasm exist in engine/build-web/ with timestamps from this run</done>
</task>

<task type="auto">
  <name>Task 2: Run diff pipeline and verify web captures show wallpaper</name>
  <files>pipeline/lib/capture-web.ts, pipeline/output/web/light.png, pipeline/output/web/dark.png, pipeline/output/diffs/light_diff.png, pipeline/output/diffs/dark_diff.png, pipeline/output/reports/light_report.html, pipeline/output/reports/dark_report.html</files>
  <action>
    Run the full diff pipeline to capture both presets and produce scored diff reports:
    ```
    npm run diff
    ```

    After the pipeline completes, inspect the web screenshots to verify the wallpaper background is visible:
    - `pipeline/output/web/light.png` — must show wallpaper background + circular glass button
    - `pipeline/output/web/dark.png` — must show wallpaper background + circular glass button

    Use the Read tool to view both PNG files visually. Confirm each shows:
    - The wallpaper background texture (not solid black or solid white)
    - A visible circular glass button in the center

    **If either web screenshot shows solid black background (wallpaper not loaded):**
    The v3.0 async wallpaper upload via `loadAndUploadWallpaper()` needs more settle time. Edit `pipeline/lib/capture-web.ts` line 43: increase `waitForTimeout(3000)` to `waitForTimeout(5000)`. Also edit `pipeline/lib/scorer.ts` line 89: increase `waitForTimeout(2000)` to `waitForTimeout(4000)`. Then re-run `npm run diff`.

    After a clean run, record the scores printed to stdout:
    - Note the light mode hybrid score (target: ≤ 0.40%)
    - Note the dark mode hybrid score (target: ≤ 0.85%)

    Include these scores in the task summary so Plan 02 knows which presets need tuning.

    Run the unit tests to confirm no regressions in the renderer internals:
    ```
    npx vitest run --reporter=verbose
    ```
    All 27 tests must pass.
  </action>
  <verify>
    <automated>ls /Users/asekar/code/glass-react/pipeline/output/web/light.png /Users/asekar/code/glass-react/pipeline/output/web/dark.png /Users/asekar/code/glass-react/pipeline/output/diffs/light_diff.png /Users/asekar/code/glass-react/pipeline/output/diffs/dark_diff.png && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Both web PNG captures exist showing wallpaper + glass button. Both diff PNGs and HTML reports exist. Diff scores printed to stdout. All 27 vitest tests pass.</done>
</task>

</tasks>

<verification>
1. `engine/build-web/glass.js` and `glass.wasm` are fresh builds from current source
2. `pipeline/output/web/light.png` and `dark.png` show wallpaper + circular glass button (not solid black)
3. `pipeline/output/diffs/light_diff.png` and `dark_diff.png` exist
4. `pipeline/output/reports/light_report.html` and `dark_report.html` exist
5. Diff scores for both presets are printed and recorded in the SUMMARY
6. `npx vitest run` exits 0 with 27 passing tests
</verification>

<success_criteria>
- WASM binary built from current source with no stale files
- Web captures visually show wallpaper background and circular glass button
- Diff pipeline exits cleanly and prints light + dark scores
- Scores recorded in SUMMARY.md for Plan 02 to act on
- v3.0 baseline scores documented: "light X.XX%, dark X.XX%"
</success_criteria>

<output>
After completion, create `.planning/phases/18-visual-validation/18-01-SUMMARY.md` following the template at `@/Users/asekar/.claude/get-shit-done/templates/summary.md`.

CRITICAL: Include the actual diff scores (light and dark hybrid %) in the SUMMARY so Plan 02 knows whether tuning is required and for which modes.
</output>
