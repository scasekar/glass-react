---
phase: 21-core-interactive-controls
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/components/controls/GlassSegmentedControl.tsx
  - src/components/controls/__tests__/GlassSegmentedControl.test.tsx
  - src/components/controls/index.ts
  - src/index.ts
autonomous: false
requirements: [CTRL-03]

must_haves:
  truths:
    - "GlassSegmentedControl displays labeled segments with one visually selected"
    - "Clicking a different segment changes the selection and moves the glass indicator"
    - "The glass indicator spring-animates between segments via motion layoutId"
    - "Arrow keys navigate between segments (roving tabindex via Radix ToggleGroup)"
    - "The segmented container is NOT a glass region -- only the indicator thumb is glass"
    - "Reduced-motion preference causes instant indicator snap (no spring)"
    - "Multiple GlassSegmentedControl instances on the same page have independent indicators"
  artifacts:
    - path: "src/components/controls/GlassSegmentedControl.tsx"
      provides: "Segmented control with glass indicator capsule"
      exports: ["GlassSegmentedControl"]
    - path: "src/components/controls/__tests__/GlassSegmentedControl.test.tsx"
      provides: "Unit tests for GlassSegmentedControl"
  key_links:
    - from: "src/components/controls/GlassSegmentedControl.tsx"
      to: "src/components/GlassPanel.tsx"
      via: "GlassPanel composition for indicator capsule only"
      pattern: "import.*GlassPanel.*from.*GlassPanel"
    - from: "src/components/controls/GlassSegmentedControl.tsx"
      to: "@radix-ui/react-toggle-group"
      via: "Radix ToggleGroup for roving tabindex and ARIA"
      pattern: "import.*ToggleGroup.*from.*radix-ui/react-toggle-group"
    - from: "src/components/controls/GlassSegmentedControl.tsx"
      to: "motion/react"
      via: "motion layoutId for spring indicator morph"
      pattern: "layoutId=.*indicator"
---

<objective>
Implement GlassSegmentedControl with spring-animated glass indicator and visual verification of all three Phase 21 controls.

Purpose: Complete the Phase 21 interactive controls set (CTRL-03) with the most visually complex control -- a segmented picker with a floating glass indicator that morphs between segments using motion's layoutId animation.
Output: Functional, tested GlassSegmentedControl; all three controls verified visually in the browser.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-core-interactive-controls/21-RESEARCH.md
@.planning/phases/21-core-interactive-controls/21-01-SUMMARY.md

@src/components/GlassPanel.tsx
@src/components/types.ts
@src/tokens/apple.ts

<interfaces>
<!-- Types created by Plan 01, available for import -->

From src/components/controls/types.ts (created in Plan 01):
```typescript
export interface GlassSegmentedControlProps {
  value: string;
  onValueChange: (value: string) => void;
  segments: Array<{ value: string; label: string }>;
  className?: string;
  style?: React.CSSProperties;
}
```

From src/components/controls/index.ts (created in Plan 01):
```typescript
export { GlassToggle } from './GlassToggle';
export { GlassSlider } from './GlassSlider';
export type { GlassToggleProps, GlassSliderProps, GlassSegmentedControlProps } from './types';
```

From src/tokens/apple.ts:
```typescript
export const APPLE_CONTROL_SIZES = Object.freeze({
  segmentedHeight: 32, segmentedPadding: 2, minTapTarget: 44,
} as const);
export const APPLE_RADII = Object.freeze({ pill: 9999 } as const);
```

Mock pattern from GlassEffectContainer.test.tsx:
```typescript
vi.mock('motion/react', () => ({
  AnimatePresence: ({ children }) => <>{children}</>,
  motion: { div: React.forwardRef(({ children, ...props }, ref) => <div ref={ref} {...props}>{children}</div>) },
  useReducedMotion: () => false,
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GlassSegmentedControl and wire exports</name>
  <files>src/components/controls/GlassSegmentedControl.tsx, src/components/controls/__tests__/GlassSegmentedControl.test.tsx, src/components/controls/index.ts, src/index.ts</files>
  <behavior>
    - Test: Renders all segment labels
    - Test: Clicking a segment calls onValueChange with that segment's value
    - Test: Clicking the already-selected segment does NOT call onValueChange (guard empty string)
    - Test: The glass indicator (GlassPanel) renders only within the selected segment
    - Test: Arrow key Right moves selection to next segment
    - Test: Each segment renders as a button with correct accessibility attributes
    - Test: Multiple instances render independently (no layoutId collision)
  </behavior>
  <action>
    1. Create `src/components/controls/__tests__/GlassSegmentedControl.test.tsx`:
       - Use `/** @vitest-environment jsdom */` docblock
       - Mock `motion/react` following the established pattern (motion.div forwardRef, useReducedMotion returning false, AnimatePresence passthrough)
       - Mock `../../../hooks/useGlassRegion` as no-op
       - Mock `../../../hooks/useGlassEngine` returning `{ ready: false, preferences: { reducedMotion: false, reducedTransparency: false, darkMode: true } }`
       - Write tests for behaviors listed above
       - For the "indicator renders in selected segment" test: render with `value="b"` and segments `[{value:"a",label:"A"},{value:"b",label:"B"},{value:"c",label:"C"}]`. Verify that the motion.div with data-testid "segment-indicator" (or by querying the GlassPanel inside the selected button) exists inside the selected segment's button container.
       - For "multiple instances": render two GlassSegmentedControl instances. Click a segment in one, verify the other's onValueChange was NOT called.
       - For "guard empty string": use `vi.fn()` as onValueChange. Simulate clicking the already-selected segment. The mock should NOT have been called (Radix ToggleGroup returns empty string on deselect, which the guard filters).

    2. Create `src/components/controls/GlassSegmentedControl.tsx`:
       - Import `* as ToggleGroup from '@radix-ui/react-toggle-group'`
       - Import `{ motion, useReducedMotion, LayoutGroup } from 'motion/react'`
       - Import `{ useId } from 'react'`
       - Import `{ GlassPanel } from '../GlassPanel'`
       - Import `{ APPLE_CONTROL_SIZES, APPLE_RADII, APPLE_SPACING } from '../../tokens/apple'`
       - Import `{ GlassSegmentedControlProps } from './types'`
       - Generate unique layoutId: `const id = useId(); const indicatorId = \`\${id}-seg-indicator\`;`
       - Spring config: `{ type: 'spring', stiffness: 400, damping: 35 }` when NOT reduced motion; `{ type: 'tween', duration: 0 }` when reduced motion
       - Composition structure:
         - Wrap in `LayoutGroup` (required for layoutId to work correctly with multiple instances)
         - `ToggleGroup.Root type="single"` receives `value`, `onValueChange={(v) => { if (v) onValueChange(v); }}` (guard empty string deselection)
         - Root style: `display: flex`, `height: segmentedHeight (32)`, `padding: segmentedPadding (2)`, `borderRadius: APPLE_RADII.pill`, `background: rgba(80, 80, 100, 0.4)`, `position: relative`
         - CRITICAL: The container is NOT glass (no GlassPanel) -- it is a semi-transparent tinted capsule
         - Map over `segments`, rendering `ToggleGroup.Item` for each with `value={seg.value}`:
           - Each item is a `<button>` with `position: relative`, `flex: 1`, `background: none`, `border: none`, `cursor: pointer`, `color: inherit`, `zIndex: 1`, `padding: 0 APPLE_SPACING.sm`
           - Inside each item, conditionally render the glass indicator when `value === seg.value`:
             ```
             {value === seg.value && (
               <motion.div
                 layoutId={indicatorId}
                 transition={spring}
                 style={{ position: 'absolute', inset: 0, borderRadius: APPLE_RADII.pill, zIndex: 0 }}
               >
                 <GlassPanel cornerRadius={APPLE_RADII.pill} style={{ width: '100%', height: '100%' }} />
               </motion.div>
             )}
             ```
           - Label `<span>` with `position: relative`, `zIndex: 1` to appear above indicator
       - GPU regions: Only 1 per segmented control (the glass indicator GlassPanel)

    3. Update `src/components/controls/index.ts`: add `export { GlassSegmentedControl } from './GlassSegmentedControl'`

    4. Update `src/index.ts`: add `GlassSegmentedControl` to the controls export line and `GlassSegmentedControlProps` to the types export.

    5. Run all control tests: `npx vitest run src/components/controls --reporter=verbose`
    6. Run full test suite: `npx vitest run --reporter=verbose`
    7. Run TypeScript check: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>npx vitest run src/components/controls --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>GlassSegmentedControl renders labeled segments, glass indicator morphs to selected segment, arrow keys navigate, guard prevents deselection, all control tests pass, TypeScript compiles, all three controls exported from library.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all three interactive glass controls</name>
  <action>
    Human verifies all three Phase 21 controls visually in the browser.

    What was built: GlassToggle (spring-animated switch), GlassSlider (continuous value with glass track/fill/thumb), and GlassSegmentedControl (glass indicator morphing between segments). All controls use Radix primitives for accessibility and motion springs for animation.

    Steps to verify:
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:5173 in Chrome
    3. The controls should be visible on the page (they need to be added to the demo/App.tsx -- if not yet wired into a visible page, temporarily add them to verify):
       - GlassToggle: Verify 51x31px switch with glass track. Click to toggle -- thumb should spring-animate to the other side. When ON, track shows green overlay. Press Space key -- should toggle.
       - GlassSlider: Verify glass track with fill and round thumb. Drag thumb -- value should update smoothly. Press arrow keys -- value should step. Fill should grow/shrink with value.
       - GlassSegmentedControl: Verify labeled segments in a capsule container. Click different segments -- glass indicator should spring-animate between them. Arrow keys should move selection. Container should NOT have glass effect (only the indicator is glass).
    4. Verify reduced-motion: Enable "Reduce motion" in OS accessibility settings. Repeat interactions -- all animations should snap instantly with no spring overshoot.
    5. Verify all three controls look visually cohesive with glass effects active.
  </action>
  <verify>Human confirms visual quality and interaction behavior</verify>
  <done>All three controls visually verified: glass effects render, spring animations work, keyboard navigation works, reduced-motion preference respected.</done>
</task>

</tasks>

<verification>
1. All control unit tests pass: `npx vitest run src/components/controls --reporter=verbose`
2. Full test suite passes: `npx vitest run --reporter=verbose`
3. TypeScript compiles: `npx tsc --noEmit`
4. All three controls exported from `src/index.ts`
5. Visual verification: controls render with glass effects, animations work, keyboard accessible
</verification>

<success_criteria>
- GlassSegmentedControl renders labeled segments in a non-glass capsule container
- Glass indicator capsule springs between segments on selection change
- Arrow keys navigate between segments (roving tabindex)
- Guard prevents deselection (clicking active segment is a no-op)
- Multiple instances work independently (no layoutId collision)
- Reduced-motion causes instant snap
- All three Phase 21 controls pass unit tests
- All three controls visually verified in browser
- Full library exports updated
</success_criteria>

<output>
After completion, create `.planning/phases/21-core-interactive-controls/21-02-SUMMARY.md`
</output>
