---
phase: 21-core-interactive-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/controls/types.ts
  - src/components/controls/GlassToggle.tsx
  - src/components/controls/__tests__/GlassToggle.test.tsx
  - src/components/controls/GlassSlider.tsx
  - src/components/controls/__tests__/GlassSlider.test.tsx
  - src/components/controls/index.ts
  - src/index.ts
autonomous: true
requirements: [CTRL-01, CTRL-02]

must_haves:
  truths:
    - "GlassToggle renders at Apple's 51x31px dimensions with a spring-animated glass thumb"
    - "Toggling GlassToggle updates the bound checked state"
    - "GlassToggle is keyboard accessible (Space key toggles, Tab to focus)"
    - "GlassSlider thumb tracks pointer drag and updates value continuously"
    - "GlassSlider clamps value within min/max range"
    - "GlassSlider is keyboard accessible (Arrow keys step value)"
    - "Both controls respect reduced-motion (instant snap, no spring overshoot)"
    - "Both controls respect reduced-transparency (solid fallback via GlassPanel composition)"
  artifacts:
    - path: "src/components/controls/types.ts"
      provides: "GlassToggle and GlassSlider prop interfaces"
      exports: ["GlassToggleProps", "GlassSliderProps", "GlassSegmentedControlProps"]
    - path: "src/components/controls/GlassToggle.tsx"
      provides: "Toggle switch with glass track and thumb"
      exports: ["GlassToggle"]
    - path: "src/components/controls/GlassSlider.tsx"
      provides: "Slider with glass track, fill, and thumb"
      exports: ["GlassSlider"]
    - path: "src/components/controls/__tests__/GlassToggle.test.tsx"
      provides: "Unit tests for GlassToggle"
    - path: "src/components/controls/__tests__/GlassSlider.test.tsx"
      provides: "Unit tests for GlassSlider"
    - path: "src/components/controls/index.ts"
      provides: "Barrel export for controls directory"
      exports: ["GlassToggle", "GlassSlider"]
  key_links:
    - from: "src/components/controls/GlassToggle.tsx"
      to: "src/components/GlassPanel.tsx"
      via: "GlassPanel composition for track and thumb"
      pattern: "import.*GlassPanel.*from.*GlassPanel"
    - from: "src/components/controls/GlassToggle.tsx"
      to: "@radix-ui/react-switch"
      via: "Radix Switch for ARIA switch role and keyboard"
      pattern: "import.*Switch.*from.*radix-ui/react-switch"
    - from: "src/components/controls/GlassSlider.tsx"
      to: "src/components/GlassPanel.tsx"
      via: "GlassPanel composition for track and fill"
      pattern: "import.*GlassPanel.*from.*GlassPanel"
    - from: "src/components/controls/GlassSlider.tsx"
      to: "@radix-ui/react-slider"
      via: "Radix Slider for ARIA slider role, arrow keys, pointer capture"
      pattern: "import.*Slider.*from.*radix-ui/react-slider"
    - from: "src/index.ts"
      to: "src/components/controls/index.ts"
      via: "Re-export controls from library barrel"
      pattern: "export.*from.*components/controls"
---

<objective>
Implement GlassToggle and GlassSlider controls with spring animations, glass composition, and full accessibility.

Purpose: Deliver the first two interactive glass controls (CTRL-01, CTRL-02) that users can toggle and drag, establishing the Radix + motion + GlassPanel composition pattern for all future controls.
Output: Two functional, tested, accessible glass controls exported from the library.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-core-interactive-controls/21-RESEARCH.md

@src/components/GlassPanel.tsx
@src/components/GlassButton.tsx
@src/components/types.ts
@src/tokens/apple.ts
@src/index.ts
@src/components/__tests__/GlassEffectContainer.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/tokens/apple.ts:
```typescript
export const APPLE_CONTROL_SIZES = Object.freeze({
  toggleWidth: 51, toggleHeight: 31, toggleThumbSize: 27, toggleThumbInset: 2,
  sliderTrackHeight: 4, sliderThumbSize: 28,
  segmentedHeight: 32, segmentedPadding: 2,
  minTapTarget: 44,
} as const);

export const APPLE_RADII = Object.freeze({
  sm: 8, md: 14, lg: 20, xl: 28, pill: 9999,
} as const);
```

From src/components/types.ts:
```typescript
export interface GlassStyleProps { blur?: number; opacity?: number; cornerRadius?: number; tint?: GlassColor; /* ... */ }
export interface GlassPanelProps extends GlassStyleProps { children?: React.ReactNode; className?: string; style?: React.CSSProperties; ref?: React.Ref<HTMLDivElement>; }
```

From src/components/GlassPanel.tsx:
```typescript
export function GlassPanel({ children, className, style, ref, blur, opacity, cornerRadius, tint, /* ...rest glass props */ }: GlassPanelProps)
```

From src/components/__tests__/GlassEffectContainer.test.tsx (mock pattern):
```typescript
/** @vitest-environment jsdom */
vi.mock('motion/react', () => ({
  AnimatePresence: ({ children }) => <>{children}</>,
  motion: { div: React.forwardRef(({ children, ...props }, ref) => <div ref={ref} {...props}>{children}</div>) },
  useReducedMotion: () => false,
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define control types and implement GlassToggle</name>
  <files>src/components/controls/types.ts, src/components/controls/GlassToggle.tsx, src/components/controls/__tests__/GlassToggle.test.tsx, src/components/controls/index.ts</files>
  <behavior>
    - Test: GlassToggle renders with correct dimensions (51px wide, 31px tall track)
    - Test: Clicking toggle calls onCheckedChange with opposite boolean
    - Test: Toggle renders with role="switch" and correct aria-checked
    - Test: Space key toggles the switch when focused
    - Test: Disabled toggle does not respond to clicks
    - Test: Toggle ON state shows green background color on track
  </behavior>
  <action>
    1. Create `src/components/controls/types.ts` with interfaces for ALL three Phase 21 controls (GlassToggle, GlassSlider, GlassSegmentedControl) so Plan 02 can use them directly:
       - `GlassToggleProps`: `checked: boolean`, `onCheckedChange: (checked: boolean) => void`, `label: string`, `disabled?: boolean`, `className?: string`, `style?: React.CSSProperties`
       - `GlassSliderProps`: `value: number`, `onValueChange: (value: number) => void`, `min?: number` (default 0), `max?: number` (default 100), `step?: number` (default 1), `label: string`, `disabled?: boolean`, `className?: string`, `style?: React.CSSProperties`
       - `GlassSegmentedControlProps`: `value: string`, `onValueChange: (value: string) => void`, `segments: Array<{ value: string; label: string }>`, `className?: string`, `style?: React.CSSProperties`

    2. Create `src/components/controls/__tests__/GlassToggle.test.tsx`:
       - Use `/** @vitest-environment jsdom */` docblock
       - Mock `motion/react` following GlassEffectContainer.test.tsx pattern (include `motion.div` forwardRef mock AND `useReducedMotion` returning false)
       - Mock `../../../hooks/useGlassRegion` as a no-op (controls compose GlassPanel, which calls useGlassRegion -- we just need it to not crash in jsdom)
       - Mock `../../../hooks/useGlassEngine` returning `{ ready: false, preferences: { reducedMotion: false, reducedTransparency: false, darkMode: true } }`
       - Write tests for the behaviors listed above using `@testing-library/react` and `@testing-library/user-event`
       - Import GlassToggle from the component file (not barrel -- barrel does not exist yet at test-write time)

    3. Create `src/components/controls/GlassToggle.tsx`:
       - Import `* as Switch from '@radix-ui/react-switch'`
       - Import `{ motion, useReducedMotion } from 'motion/react'`
       - Import `{ GlassPanel } from '../GlassPanel'`
       - Import `{ APPLE_CONTROL_SIZES, APPLE_RADII } from '../../tokens/apple'`
       - Import `{ GlassToggleProps } from './types'`
       - Composition structure (from research Pattern 1):
         - `Switch.Root` wraps everything, receives `checked`, `onCheckedChange`, `disabled`, `aria-label={label}`
         - Inside: a wrapper div with `minHeight: APPLE_CONTROL_SIZES.minTapTarget` for touch target
         - Inside wrapper: `GlassPanel` for track at `width: toggleWidth (51)`, `height: toggleHeight (31)`, `cornerRadius: APPLE_RADII.pill`
         - Track GlassPanel gets `position: relative` and conditional green background: when `checked`, add a sibling/overlay div with `background: rgba(52, 199, 89, 0.6)` and `borderRadius: APPLE_RADII.pill`, `position: absolute`, `inset: 0`, behind the thumb
         - Inside track: `Switch.Thumb asChild` wrapping a `motion.div` with `layout` prop and `transition={springConfig}`
         - Inside motion.div: `GlassPanel` with `cornerRadius: APPLE_RADII.pill`, `width: toggleThumbSize (27)`, `height: toggleThumbSize (27)`
         - Thumb position: `position: absolute`, `top: toggleThumbInset (2)`, `left: checked ? (toggleWidth - toggleThumbSize - toggleThumbInset) : toggleThumbInset`
       - Spring config: `{ type: 'spring', stiffness: 500, damping: 30, mass: 0.8 }` when NOT reduced motion; `{ type: 'tween', duration: 0 }` when reduced motion
       - CRITICAL: Do NOT call `useGlassRegion` directly -- compose GlassPanel which handles it

    4. Create `src/components/controls/index.ts` barrel with `export { GlassToggle } from './GlassToggle'` and types re-exports. Leave placeholder comment for GlassSlider and GlassSegmentedControl (added in next task and Plan 02).

    5. Run tests: `npx vitest run src/components/controls/__tests__/GlassToggle.test.tsx`
  </action>
  <verify>
    <automated>npx vitest run src/components/controls/__tests__/GlassToggle.test.tsx --reporter=verbose</automated>
  </verify>
  <done>GlassToggle renders at 51x31px with spring-animated thumb, responds to click and Space key, shows green ON state, disabled state works, all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GlassSlider and wire library exports</name>
  <files>src/components/controls/GlassSlider.tsx, src/components/controls/__tests__/GlassSlider.test.tsx, src/components/controls/index.ts, src/index.ts</files>
  <behavior>
    - Test: GlassSlider renders with role="slider" and correct aria-valuenow/min/max
    - Test: GlassSlider calls onValueChange when value changes
    - Test: GlassSlider renders track and thumb elements
    - Test: Arrow key Right increments value by step
    - Test: Arrow key Left decrements value by step
    - Test: Disabled slider does not respond to interaction
  </behavior>
  <action>
    1. Create `src/components/controls/__tests__/GlassSlider.test.tsx`:
       - Use `/** @vitest-environment jsdom */` docblock
       - Same mock pattern as GlassToggle test (mock `motion/react`, `useGlassRegion`, `useGlassEngine`)
       - Write tests for behaviors listed above
       - NOTE on Radix Slider: Radix Slider renders its own DOM structure. The `role="slider"` is on the Thumb element. Query using `screen.getByRole('slider')` for the thumb.
       - For arrow key tests: focus the slider thumb element, then use `await user.keyboard('{ArrowRight}')`. Verify `onValueChange` was called with `value + step`.

    2. Create `src/components/controls/GlassSlider.tsx`:
       - Import `* as RadixSlider from '@radix-ui/react-slider'`
       - Import `{ GlassPanel } from '../GlassPanel'`
       - Import `{ APPLE_CONTROL_SIZES, APPLE_RADII } from '../../tokens/apple'`
       - Import `{ GlassSliderProps } from './types'`
       - Composition structure (from research):
         - `RadixSlider.Root` receives `value={[value]}`, `onValueChange={([v]) => onValueChange(v)}`, `min`, `max`, `step`, `aria-label={label}`, `disabled`
         - Root style: `position: relative`, `display: flex`, `alignItems: center`, `width: 100%`, `height: minTapTarget (44)` for touch target
         - `RadixSlider.Track asChild` wrapping `GlassPanel` for the track at `height: sliderTrackHeight (4)`, `cornerRadius: APPLE_RADII.pill`, `flexGrow: 1`, `position: relative`
         - Inside track: `RadixSlider.Range asChild` wrapping `GlassPanel` for the fill at `position: absolute`, `height: 100%`, `cornerRadius: APPLE_RADII.pill`
         - `RadixSlider.Thumb asChild` wrapping a `GlassPanel` (NOT GlassButton per research Open Question 2 -- GlassPanel renders div, which is correct for slider thumb) at `width: sliderThumbSize (28)`, `height: sliderThumbSize (28)`, `cornerRadius: APPLE_RADII.pill`, `display: block`
       - NOTE: Do NOT add motion animation to the slider thumb -- Radix Slider handles thumb positioning via CSS transform. Adding motion layout animation creates double-transform conflicts (research anti-pattern).
       - GPU regions: Track (1) + Fill (1) + Thumb (1) = 3 regions per slider instance

    3. Update `src/components/controls/index.ts`: add `export { GlassSlider } from './GlassSlider'` and export all types from `./types`.

    4. Update `src/index.ts`: add exports for the new controls:
       ```
       // Controls
       export { GlassToggle, GlassSlider } from './components/controls';
       export type { GlassToggleProps, GlassSliderProps, GlassSegmentedControlProps } from './components/controls';
       ```

    5. Run all control tests: `npx vitest run src/components/controls`
  </action>
  <verify>
    <automated>npx vitest run src/components/controls --reporter=verbose</automated>
  </verify>
  <done>GlassSlider renders with glass track/fill/thumb, responds to arrow keys, calls onValueChange, all control tests pass, GlassToggle and GlassSlider exported from library index.</done>
</task>

</tasks>

<verification>
1. All control unit tests pass: `npx vitest run src/components/controls --reporter=verbose`
2. Full test suite still passes: `npx vitest run --reporter=verbose`
3. TypeScript compiles without errors: `npx tsc --noEmit`
4. Library exports are importable: check `src/index.ts` includes GlassToggle, GlassSlider
</verification>

<success_criteria>
- GlassToggle renders at 51x31px with glass track and 27px glass thumb
- Toggle thumb spring-animates on state change (uses motion layout)
- GlassSlider renders glass track (4px), glass fill, and glass thumb (28px)
- Slider value updates via pointer drag and arrow keys
- Both controls use Radix primitives for ARIA roles and keyboard nav
- Both controls respect reduced-motion (instant snap) via useReducedMotion
- Both controls respect reduced-transparency (solid fallback) via GlassPanel composition
- All unit tests pass
- Controls exported from src/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/21-core-interactive-controls/21-01-SUMMARY.md`
</output>
