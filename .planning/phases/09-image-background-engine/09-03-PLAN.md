---
phase: 09-image-background-engine
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [IMG-01, IMG-02, IMG-03, IMG-04]

must_haves:
  truths:
    - "ROADMAP.md Phase 9 success criterion 1 accurately reflects what was built: the bundled wallpaper renders as the default background behind glass components with correct refraction (no custom URL prop)"
    - "ROADMAP.md Phase 9 success criterion 3 is removed or merged since it is redundant with the updated criterion 1"
    - "REQUIREMENTS.md correctly marks IMG-03 as complete in both checkbox and traceability table"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Accurate Phase 9 success criteria matching bundled-wallpaper-only scope"
      contains: "bundled default wallpaper"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate completion status for IMG-03"
      contains: "\\[x\\] \\*\\*IMG-03\\*\\*"
  key_links: []
---

<objective>
Close documentation gaps from 09-VERIFICATION.md: (1) update ROADMAP.md Phase 9 success criterion 1 to match what was actually built (bundled wallpaper only, no backgroundSrc prop -- which was explicitly deferred by the user), and (2) verify REQUIREMENTS.md IMG-03 status is correct.

Purpose: The current ROADMAP success criterion 1 references a `backgroundSrc` prop that was explicitly deferred in 09-CONTEXT.md. The criterion must be rewritten to reflect the actual Phase 9 scope: the bundled wallpaper renders as the default background behind glass components with correct refraction. Criterion 3 (default wallpaper fallback when no backgroundSrc is provided) becomes redundant once criterion 1 is updated and should be removed or merged. The REQUIREMENTS.md fix is a documentation accuracy verification.

Output: Updated ROADMAP.md with accurate Phase 9 success criteria, verified REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/asekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-image-background-engine/09-CONTEXT.md
@.planning/phases/09-image-background-engine/09-02-SUMMARY.md
@.planning/phases/09-image-background-engine/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 9 success criteria to reflect bundled-wallpaper-only scope</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <action>
**Gap being closed:** ROADMAP success criterion 1 references `backgroundSrc` prop which was explicitly deferred by the user in 09-CONTEXT.md. The criteria must match what was actually built.

**Read `.planning/ROADMAP.md` and locate the Phase 9 `**Success Criteria**` block.** It currently reads:

```
**Success Criteria** (what must be TRUE):
  1. User can pass a `backgroundSrc` URL to GlassProvider and see that image rendered behind glass components with correct refraction
  2. User can switch between `backgroundMode="noise"` and `backgroundMode="image"` without reloading the page, and both modes render correctly
  3. When no `backgroundSrc` is provided, the library's bundled default wallpaper image loads automatically in image mode
  4. A 50% gray test image passed through the glass pipeline with zero effects produces 50% gray output (sRGB/linear color space correctness verified)
```

**Replace the four criteria with these three:**

```
**Success Criteria** (what must be TRUE):
  1. The library's bundled default wallpaper renders as the background behind glass components with correct refraction in image mode
  2. User can switch between `backgroundMode="noise"` and `backgroundMode="image"` without reloading the page, and both modes render correctly
  3. A 50% gray test image passed through the glass pipeline with zero effects produces 50% gray output (sRGB/linear color space correctness verified)
```

**What changed and why:**
- Criterion 1: Rewritten to reflect actual scope -- bundled wallpaper only, no `backgroundSrc` prop (deferred per user decision in 09-CONTEXT.md)
- Criterion 2: Unchanged (mode switching was implemented and verified)
- Old criterion 3: Removed -- it described fallback behavior for a `backgroundSrc` prop that does not exist; the bundled wallpaper is now criterion 1
- Old criterion 4 becomes new criterion 3: Unchanged (sRGB correctness)

**Do NOT change anything else in ROADMAP.md.** Only the four success criteria lines for Phase 9 are modified.
  </action>
  <verify>
    <automated>grep -A 4 "Success Criteria" /Users/asekar/code/glass-react/.planning/ROADMAP.md | head -8</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 9 success criterion 1 says "bundled default wallpaper renders as the background" (no backgroundSrc reference)
    - Old criterion 3 (backgroundSrc fallback) is removed
    - Criteria renumbered to 1-3 (was 1-4)
    - No other ROADMAP content changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify REQUIREMENTS.md IMG-03 status is correct</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
**Gap being closed:** REQUIREMENTS.md documentation accuracy -- IMG-03 checkbox should reflect actual implementation status.

**Check current state first.** Read `.planning/REQUIREMENTS.md` and verify:
1. Line with `**IMG-03**` -- should be `- [x] **IMG-03**` (not `- [ ] **IMG-03**`)
2. Traceability table row for IMG-03 -- should be `| IMG-03 | Phase 9 | Complete |` (not `Pending`)

**If already correct (both checkbox and table show complete):** No changes needed. Report as already accurate in the summary.

**If still showing `[ ]` or `Pending`:**
- Change `- [ ] **IMG-03**` to `- [x] **IMG-03**`
- Change `| IMG-03 | Phase 9 | Pending |` to `| IMG-03 | Phase 9 | Complete |`

This is a documentation-only verification. The implementation (src/assets/wallpaper.jpg bundled via Vite) was verified complete in 09-VERIFICATION.md.

**Note:** Based on pre-read, REQUIREMENTS.md already shows `[x]` and `Complete` for IMG-03. This task exists to formally verify and confirm; no edit is expected.
  </action>
  <verify>
    <automated>grep -n "IMG-03" /Users/asekar/code/glass-react/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - REQUIREMENTS.md IMG-03 checkbox shows [x] (complete)
    - REQUIREMENTS.md traceability table shows IMG-03 as Complete
    - Documentation accurately reflects implementation status
  </done>
</task>

</tasks>

<verification>
1. ROADMAP.md Phase 9 success criterion 1 references bundled wallpaper, NOT backgroundSrc
2. ROADMAP.md Phase 9 has exactly 3 success criteria (not 4)
3. No mention of `backgroundSrc` in any ROADMAP.md Phase 9 success criterion
4. REQUIREMENTS.md shows IMG-03 as [x] Complete in both checkbox and traceability table
5. No source code files were modified (documentation-only plan)
</verification>

<success_criteria>
- ROADMAP.md Phase 9 success criteria accurately reflect bundled-wallpaper-only scope with no reference to backgroundSrc
- REQUIREMENTS.md accurately tracks IMG-03 as complete
- Zero source code changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-image-background-engine/09-03-SUMMARY.md`
</output>
